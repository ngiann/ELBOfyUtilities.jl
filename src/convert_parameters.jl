function diagonal_parameters(_,_)

    @error("package ELBOfy must be independently loaded!")
    
end


function full_parameters(_,_)

    @error("package ELBOfy must be independently loaded!")
    
end


function mvi_parameters(_,_)

    @error("package ELBOfy must be independently loaded!")
    
end